<!DOCTYPE html>
<html lang="en" data-theme="light">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= country.commonName %> - Global Nations</title>
    <link href="https://cdn.jsdelivr.net/npm/daisyui@4.4.19/dist/full.min.css" rel="stylesheet" type="text/css" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <!-- Navbar -->
    <div class="navbar bg-base-100">
        <div class="flex-1">
            <a href="/" class="btn btn-ghost text-xl">Global Nations</a>
        </div>
        <div class="flex-none">
            <ul class="menu menu-horizontal px-1">
                <li><a href="/">Featured Countries</a></li>
                <li><a href="/about">About</a></li>
            </ul>
        </div>
    </div>

    <main class="container mx-auto px-4 py-8">
        <div class="card bg-base-100 shadow-xl max-w-3xl mx-auto">
            <div class="card-body">
                <h2 class="card-title text-3xl mb-4">
                    <%= country.commonName %> <%= country.flag %>
                </h2>
                <div class="space-y-3">
                    <p><strong>Official Name:</strong> <%= country.officialName %></p>
                    <p><strong>Capital:</strong> <%= country.capital %></p>
                    <p><strong>Region:</strong> <%= country.SubRegion.region %></p>
                    <p><strong>Sub-Region:</strong> <%= country.SubRegion.subRegion %></p>
                    <p><strong>Population:</strong> <%= country.population.toLocaleString() %></p>
                    <p><strong>Area:</strong> <%= country.area.toLocaleString() %> km²</p>
                    <p><strong>Languages:</strong> <%= country.languages %></p>
                    <p><strong>Currencies:</strong> <%= country.currencies %></p>
                    <p><strong>Landlocked:</strong> <%= country.landlocked ? 'Yes' : 'No' %></p>
                    <% if (country.openStreetMaps) { %>
                        <p><strong>Map:</strong> 
                            <a href="<%= country.openStreetMaps %>" target="_blank" class="link link-primary">
                                View on OpenStreetMap
                            </a>
                        </p>
                    <% } %>
                </div>
                <div class="card-actions justify-end mt-6">
                    <a href="/" class="btn btn-primary">Back to Featured Countries</a>
                </div>
            </div>
        </div>
    </main>

    <!-- Footer -->
    <footer class="footer footer-center p-10 bg-base-200 text-base-content rounded">
        <nav class="grid grid-flow-col gap-4">
            <a href="/about">About</a>
        </nav> 
        <aside>
            <p>Copyright © 2024 - All right reserved</p>
        </aside>
    </footer>
</body>
</html> 